import React from 'react';
import { View } from 'react-native';
import Formulario from './Formulario';

export default () => (
  <View style={{ flex: 1, paddingTop: 50 }}>
    <Formulario type='edicao' />
  </View>
);
